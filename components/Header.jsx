const Header = () => (
  <div className="header">
    <nav>
      <ul>
        <li>Sign In</li>
      </ul>
    </nav>
    <style jsx>{`
      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }
    `}</style>
  </div>

);

export default Header;
